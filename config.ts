export type ConfigProps = {
  appDescription: string;
  appName: string;
  domainName: string;
};

const config = {
  appName: 'sanaam',
  appDescription: '',
  domainName: 'sanaam.sa',
} as ConfigProps;

export default config;
